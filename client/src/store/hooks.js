import { useDispatch, useSelector } from "react-redux";

// Typed hooks untuk TypeScript-like experience
export const useAppDispatch = () => useDispatch();
export const useAppSelector = useSelector;

